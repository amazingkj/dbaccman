services:
  dbaccman:
    image: jiin724/dbaccman:latest
    container_name: dbaccman
    ports:
      - "3080:3080"
    environment:
      - JWT_SECRET=${JWT_SECRET:-change-this-in-production}
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
